// Targeted by JavaCPP version 1.5.3-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.arrow_dataset;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.arrow.*;
import static org.bytedeco.arrow.global.arrow.*;
import org.bytedeco.parquet.*;
import static org.bytedeco.arrow.global.parquet.*;

import static org.bytedeco.arrow.global.arrow_dataset.*;


/** \brief A DataSource which takes files of one format from a directory
 * 
 *  The directory is crawled upon construction (Make) and not updated afterward.
 *  GetFragments() will not include files added after this DataDource is constructed and
 *  will error if files are deleted/moved. */
@Namespace("arrow::dataset") @NoOffset @Properties(inherit = org.bytedeco.arrow.presets.arrow_dataset.class)
public class FileSystemBasedDataSource extends DataSource {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public FileSystemBasedDataSource(Pointer p) { super(p); }

  public static native @ByVal Status Make(FileSystem filesystem, @Const @ByRef Selector selector,
                       @SharedPtr FileFormat format,
                       @UniquePtr FileSystemBasedDataSource out);

  public static native @ByVal Status Make(FileSystem filesystem, @Const @ByRef Selector selector,
                       @SharedPtr FileFormat format,
                       @SharedPtr Expression partition_expression,
                       @UniquePtr FileSystemBasedDataSource out);

  public native @StdString String type();
}
