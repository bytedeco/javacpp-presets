// Targeted by JavaCPP version 1.5.7: DO NOT EDIT THIS FILE

package org.bytedeco.arrow;

import org.bytedeco.arrow.Function;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.arrow.global.arrow.*;


/** \brief Wait for one of the futures to end, or for the given timeout to expire.
 * 
 *  The indices of all completed futures are returned.  Note that some futures
 *  may not be in the returned set, but still complete concurrently. */

@Namespace("arrow") @Properties(inherit = org.bytedeco.arrow.presets.arrow.class)
public class Continue extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public Continue() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public Continue(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Continue(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public Continue position(long position) {
        return (Continue)super.position(position);
    }
    @Override public Continue getPointer(long i) {
        return new Continue((Pointer)this).offsetAddress(i);
    }

  
}
