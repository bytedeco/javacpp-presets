// Targeted by JavaCPP version 1.5.7: DO NOT EDIT THIS FILE

package org.bytedeco.depthai;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.opencv.opencv_core.*;
import static org.bytedeco.opencv.global.opencv_core.*;
import org.bytedeco.opencv.opencv_imgproc.*;
import static org.bytedeco.opencv.global.opencv_imgproc.*;

import static org.bytedeco.depthai.global.depthai.*;

@Name("std::vector<std::shared_ptr<dai::ADatatype> >") @Properties(inherit = org.bytedeco.depthai.presets.depthai.class)
public class ADatatypeVector extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ADatatypeVector(Pointer p) { super(p); }
    public ADatatypeVector(ADatatype value) { this(1); put(0, value); }
    public ADatatypeVector(ADatatype ... array) { this(array.length); put(array); }
    public ADatatypeVector()       { allocate();  }
    public ADatatypeVector(long n) { allocate(n); }
    private native void allocate();
    private native void allocate(@Cast("size_t") long n);
    public native @Name("operator =") @ByRef ADatatypeVector put(@ByRef ADatatypeVector x);

    public boolean empty() { return size() == 0; }
    public native long size();
    public void clear() { resize(0); }
    public native void resize(@Cast("size_t") long n);

    @Index(function = "at") public native @SharedPtr ADatatype get(@Cast("size_t") long i);
    public native ADatatypeVector put(@Cast("size_t") long i, ADatatype value);

    public native @ByVal Iterator insert(@ByVal Iterator pos, @SharedPtr ADatatype value);
    public native @ByVal Iterator erase(@ByVal Iterator pos);
    public native @ByVal Iterator begin();
    public native @ByVal Iterator end();
    @NoOffset @Name("iterator") public static class Iterator extends Pointer {
        public Iterator(Pointer p) { super(p); }
        public Iterator() { }

        public native @Name("operator ++") @ByRef Iterator increment();
        public native @Name("operator ==") boolean equals(@ByRef Iterator it);
        public native @Name("operator *") @SharedPtr @Const ADatatype get();
    }

    public ADatatype[] get() {
        ADatatype[] array = new ADatatype[size() < Integer.MAX_VALUE ? (int)size() : Integer.MAX_VALUE];
        for (int i = 0; i < array.length; i++) {
            array[i] = get(i);
        }
        return array;
    }
    @Override public String toString() {
        return java.util.Arrays.toString(get());
    }

    public ADatatype pop_back() {
        long size = size();
        ADatatype value = get(size - 1);
        resize(size - 1);
        return value;
    }
    public ADatatypeVector push_back(ADatatype value) {
        long size = size();
        resize(size + 1);
        return put(size, value);
    }
    public ADatatypeVector put(ADatatype value) {
        if (size() != 1) { resize(1); }
        return put(0, value);
    }
    public ADatatypeVector put(ADatatype ... array) {
        if (size() != array.length) { resize(array.length); }
        for (int i = 0; i < array.length; i++) {
            put(i, array[i]);
        }
        return this;
    }
}

