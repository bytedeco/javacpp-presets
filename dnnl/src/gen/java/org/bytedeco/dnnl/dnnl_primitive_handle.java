// Targeted by JavaCPP version 1.5.2: DO NOT EDIT THIS FILE

package org.bytedeco.dnnl;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.dnnl.global.dnnl.*;

@Name("dnnl::handle<dnnl_primitive_t>") @NoOffset @Properties(inherit = org.bytedeco.dnnl.presets.dnnl.class)
public class dnnl_primitive_handle extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public dnnl_primitive_handle(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public dnnl_primitive_handle(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public dnnl_primitive_handle position(long position) {
        return (dnnl_primitive_handle)super.position(position);
    }

    /** Empty constructor.
     * 
     *  Allows declaring an object before actual initialization
     *  (mostly for convenience).
     * 
     *  \warning
     *      Uninitialized object cannot be used in any library calls.
     *      Any attempt to use its methods or passing it to the other library
     *      function will lead to a thrown exception. */
    public dnnl_primitive_handle() { super((Pointer)null); allocate(); }
    private native void allocate();
    public dnnl_primitive_handle(@Const @ByRef dnnl_primitive_handle arg0) { super((Pointer)null); allocate(arg0); }
    private native void allocate(@Const @ByRef dnnl_primitive_handle arg0);
    public native @ByRef @Name("operator =") dnnl_primitive_handle put(@ByVal dnnl_primitive_handle arg0);

    /** Constructs a C handle wrapper from a C handle.
     *  @param t The C handle to wrap.
     *  @param weak A flag to specify whether to construct a weak wrapper. */
    public dnnl_primitive_handle(dnnl_primitive t, @Cast("bool") boolean weak/*=false*/) { super((Pointer)null); allocate(t, weak); }
    private native void allocate(dnnl_primitive t, @Cast("bool") boolean weak/*=false*/);
    public dnnl_primitive_handle(dnnl_primitive t) { super((Pointer)null); allocate(t); }
    private native void allocate(dnnl_primitive t);

    /** Resets the value of a C handle.
     *  @param t The new value of the C handle.
     *  @param weak A flag to specify whether the wrapper should be weak. */
    public native void reset(dnnl_primitive t, @Cast("bool") boolean weak/*=false*/);
    public native void reset(dnnl_primitive t);

    /** Returns the value of the underlying C handle. */
    public native dnnl_primitive get(@Cast("bool") boolean allow_emtpy/*=false*/);
    public native dnnl_primitive get();

    public native @Name("operator dnnl_primitive_t") dnnl_primitive asDnnl_primitive();

    public native @Cast("bool") @Name("operator bool") boolean asBoolean();

    public native @Cast("bool") @Name("operator ==") boolean equals(@Const @ByRef dnnl_primitive_handle other);
    public native @Cast("bool") @Name("operator !=") boolean notEquals(@Const @ByRef dnnl_primitive_handle other);
}
