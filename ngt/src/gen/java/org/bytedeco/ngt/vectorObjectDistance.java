// Targeted by JavaCPP version 1.5.7-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ngt;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ngt.global.ngt.*;

@Name("std::vector<NGT::ObjectDistance>") @Properties(inherit = org.bytedeco.ngt.presets.ngt.class)
public class vectorObjectDistance extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public vectorObjectDistance(Pointer p) { super(p); }
    public vectorObjectDistance(ObjectDistance value) { this(1); put(0, value); }
    public vectorObjectDistance(ObjectDistance ... array) { this(array.length); put(array); }
    public vectorObjectDistance()       { allocate();  }
    public vectorObjectDistance(long n) { allocate(n); }
    private native void allocate();
    private native void allocate(@Cast("size_t") long n);
    public native @Name("operator =") @ByRef vectorObjectDistance put(@ByRef vectorObjectDistance x);

    public boolean empty() { return size() == 0; }
    public native long size();
    public void clear() { resize(0); }
    public native void resize(@Cast("size_t") long n);

    @Index(function = "at") public native @ByRef ObjectDistance get(@Cast("size_t") long i);
    public native vectorObjectDistance put(@Cast("size_t") long i, ObjectDistance value);

    public native @ByVal Iterator insert(@ByVal Iterator pos, @ByRef ObjectDistance value);
    public native @ByVal Iterator erase(@ByVal Iterator pos);
    public native @ByVal Iterator begin();
    public native @ByVal Iterator end();
    @NoOffset @Name("iterator") public static class Iterator extends Pointer {
        public Iterator(Pointer p) { super(p); }
        public Iterator() { }

        public native @Name("operator ++") @ByRef Iterator increment();
        public native @Name("operator ==") boolean equals(@ByRef Iterator it);
        public native @Name("operator *") @ByRef @Const ObjectDistance get();
    }

    public ObjectDistance[] get() {
        ObjectDistance[] array = new ObjectDistance[size() < Integer.MAX_VALUE ? (int)size() : Integer.MAX_VALUE];
        for (int i = 0; i < array.length; i++) {
            array[i] = get(i);
        }
        return array;
    }
    @Override public String toString() {
        return java.util.Arrays.toString(get());
    }

    public ObjectDistance pop_back() {
        long size = size();
        ObjectDistance value = get(size - 1);
        resize(size - 1);
        return value;
    }
    public vectorObjectDistance push_back(ObjectDistance value) {
        long size = size();
        resize(size + 1);
        return put(size, value);
    }
    public vectorObjectDistance put(ObjectDistance value) {
        if (size() != 1) { resize(1); }
        return put(0, value);
    }
    public vectorObjectDistance put(ObjectDistance ... array) {
        if (size() != array.length) { resize(array.length); }
        for (int i = 0; i < array.length; i++) {
            put(i, array[i]);
        }
        return this;
    }
}

