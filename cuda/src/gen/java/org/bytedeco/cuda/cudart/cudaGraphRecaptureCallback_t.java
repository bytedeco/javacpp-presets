// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.cuda.cudart;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.cuda.global.cudart.*;


/**
 * Callback function invoked when node parameter mismatches are detected while recapturing to an existing graph.
 * Parameter struct pointers are only valid within the callback.
 * @param data User parameter provided at beginning of recapture
 * @param node Graph node with the parameter mismatch
 * @param originalParams The original node parameters from the graph
 * @param recaptureParams The node parameters received during the recapture
 * @param status status Status code for the recapture of the node
 *
 * @return Error code for the callback. Anything other than cudaSuccess will cause the recapture to fail immediately.
 */
@Convention("CUDART_CB") @Properties(inherit = org.bytedeco.cuda.presets.cudart.class)
public class cudaGraphRecaptureCallback_t extends FunctionPointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public    cudaGraphRecaptureCallback_t(Pointer p) { super(p); }
    protected cudaGraphRecaptureCallback_t() { allocate(); }
    private native void allocate();
    public native @Cast("cudaError_t") int call(Pointer data,
                                                              CUgraphNode_st node,
                                                              @Const cudaGraphNodeParams originalParams,
                                                              @Const cudaGraphNodeParams recaptureParams,
                                                              @Cast("cudaGraphRecaptureStatus") int status);
}
