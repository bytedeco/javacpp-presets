// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.cuda.cupti;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import org.bytedeco.cuda.cudart.*;
import static org.bytedeco.cuda.global.cudart.*;

import static org.bytedeco.cuda.global.cupti.*;


// Structure used by users to specify which fields they want from a record type.
@Properties(inherit = org.bytedeco.cuda.presets.cupti.class)
public class CUpti_ActivityFieldSelection extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public CUpti_ActivityFieldSelection() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public CUpti_ActivityFieldSelection(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CUpti_ActivityFieldSelection(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public CUpti_ActivityFieldSelection position(long position) {
        return (CUpti_ActivityFieldSelection)super.position(position);
    }
    @Override public CUpti_ActivityFieldSelection getPointer(long i) {
        return new CUpti_ActivityFieldSelection((Pointer)this).offsetAddress(i);
    }

  // Size of this structure (used for version compatibility).
  public native @Cast("size_t") long structSize(); public native CUpti_ActivityFieldSelection structSize(long setter);

  // Number of fields requested by the user.
  public native @Cast("size_t") long numFields(); public native CUpti_ActivityFieldSelection numFields(long setter);

  // Array of field IDs (enum values) selected by the user.
  // The first field id in the selection for any CUPTI activity kind must be *_FIELD_KIND.
  public native IntPointer pFieldIds(); public native CUpti_ActivityFieldSelection pFieldIds(IntPointer setter);
}
