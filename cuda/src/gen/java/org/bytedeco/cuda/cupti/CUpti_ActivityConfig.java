// Targeted by JavaCPP version 1.5.15-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.cuda.cupti;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import org.bytedeco.cuda.cudart.*;
import static org.bytedeco.cuda.global.cudart.*;

import static org.bytedeco.cuda.global.cupti.*;

// #endif

/**
 * \brief Activity configuration
 */
@Properties(inherit = org.bytedeco.cuda.presets.cupti.class)
public class CUpti_ActivityConfig extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public CUpti_ActivityConfig() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public CUpti_ActivityConfig(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CUpti_ActivityConfig(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public CUpti_ActivityConfig position(long position) {
        return (CUpti_ActivityConfig)super.position(position);
    }
    @Override public CUpti_ActivityConfig getPointer(long i) {
        return new CUpti_ActivityConfig((Pointer)this).offsetAddress(i);
    }

  /**
   * Size of the data structure. CUPTI client should set the size of the structure.
   * It will be used in CUPTI to check what fields are available in the structure.
   * Used to preserve backward compatibility.
   */
  public native @Cast("size_t") long structSize(); public native CUpti_ActivityConfig structSize(long setter);

  /**
   * Selection of the fields to collect for the activity kind.
   */
  public native @ByRef CUpti_ActivityFieldSelection fieldSelection(); public native CUpti_ActivityConfig fieldSelection(CUpti_ActivityFieldSelection setter);

  /**
   * Optional pointer to kind-specific options. For CUPTI_ACTIVITY_KIND_RUNTIME and
   * CUPTI_ACTIVITY_KIND_DRIVER, this may point to a CUpti_ActivityApiOptions struct
   * to enable allowlist or denylist filtering of individual cbids. NULL disables
   * filtering (all cbids enabled). Ignored for all other activity kinds.
   * Available since CUPTI_API_VERSION 130400.
   */
  public native Pointer pKindOptions(); public native CUpti_ActivityConfig pKindOptions(Pointer setter);
}
