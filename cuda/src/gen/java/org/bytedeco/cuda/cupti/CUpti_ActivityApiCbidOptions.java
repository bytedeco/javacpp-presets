// Targeted by JavaCPP version 1.5.15-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.cuda.cupti;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import org.bytedeco.cuda.cudart.*;
import static org.bytedeco.cuda.global.cudart.*;

import static org.bytedeco.cuda.global.cupti.*;


/**
 * \brief Per-cbid enable/disable options for API activity kinds.
 *
 * When enable = 1 (allowlist), records are generated only for the cbids listed in pCbids.
 * When enable = 0 (denylist), records are generated for all cbids except those listed.
 *
 * @since CUPTI_API_VERSION 130400
 */
@Properties(inherit = org.bytedeco.cuda.presets.cupti.class)
public class CUpti_ActivityApiCbidOptions extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public CUpti_ActivityApiCbidOptions() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public CUpti_ActivityApiCbidOptions(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CUpti_ActivityApiCbidOptions(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public CUpti_ActivityApiCbidOptions position(long position) {
        return (CUpti_ActivityApiCbidOptions)super.position(position);
    }
    @Override public CUpti_ActivityApiCbidOptions getPointer(long i) {
        return new CUpti_ActivityApiCbidOptions((Pointer)this).offsetAddress(i);
    }

  /**
   * Size of the data structure. CUPTI client should set the size of the structure.
   * It will be used in CUPTI to check what fields are available in the structure.
   * Used to preserve backward compatibility.
   */
  public native @Cast("size_t") long structSize(); public native CUpti_ActivityApiCbidOptions structSize(long setter);

  /**
   * Array of callback IDs to allow or deny. Must not be NULL when numCbids > 0.
   */
  public native @Cast("CUpti_CallbackId*") IntPointer pCbids(); public native CUpti_ActivityApiCbidOptions pCbids(IntPointer setter);

  /**
   * Number of entries in pCbids.
   */
  public native @Cast("size_t") long numCbids(); public native CUpti_ActivityApiCbidOptions numCbids(long setter);

  /**
   * 1 = allowlist (record only the listed cbids); 0 = denylist (suppress the listed cbids).
   */
  public native @Cast("uint8_t") byte enable(); public native CUpti_ActivityApiCbidOptions enable(byte setter);
}
