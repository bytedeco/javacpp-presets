// Targeted by JavaCPP version 1.5.15-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.cuda.nccl;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import org.bytedeco.cuda.cudart.*;
import static org.bytedeco.cuda.global.cudart.*;

import static org.bytedeco.cuda.global.nccl.*;


/* A single vendor-specific option expressed as a key-value pair. Multiple options can be
 * chained into a non-circular linked list through the next pointer and attached to a ncclCollConfig_t
 * via its ext member. The official NCCL library ignores every extension; a vendor library
 * should assign a unique value to key.vendorId to avoid collision among vendor libraries.
 * It can usually be done by picking a random number.
 * Vendors should choose a non-zero vendorId (less than 2^24) that is unlikely to collide
 * with other vendor libraries. The list is unordered and must not contain duplicate keys. */
@Name("ncclConfigExt_t") @Properties(inherit = org.bytedeco.cuda.presets.nccl.class)
public class ncclConfigExt extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public ncclConfigExt() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public ncclConfigExt(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ncclConfigExt(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public ncclConfigExt position(long position) {
        return (ncclConfigExt)super.position(position);
    }
    @Override public ncclConfigExt getPointer(long i) {
        return new ncclConfigExt((Pointer)this).offsetAddress(i);
    }

  public native ncclConfigExt next(); public native ncclConfigExt next(ncclConfigExt setter);       /* next option in the list, or NULL to terminate */
    @Name("key.vendorId") public native int key_vendorId(); public native ncclConfigExt key_vendorId(int setter);                   /* vendor choose their own unique value as vendorId */
    @Name("key.optionId") public native int key_optionId(); public native ncclConfigExt key_optionId(int setter);                   /* vendor-defined id distinguishing options within a vendor */
    @Name("val.i") public native int val_i(); public native ncclConfigExt val_i(int setter);                          /* integer value */
    @Name("val.s") public native @Cast("const char*") BytePointer val_s(); public native ncclConfigExt val_s(BytePointer setter);                  /* C-style string value */
    @Name("val.raw") public native Pointer val_raw(); public native ncclConfigExt val_raw(Pointer setter);                      /* opaque pointer-sized value */
}
