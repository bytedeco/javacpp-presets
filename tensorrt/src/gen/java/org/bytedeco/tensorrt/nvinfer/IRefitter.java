// Targeted by JavaCPP version 1.5.4-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.tensorrt.nvinfer;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import org.bytedeco.cuda.cudart.*;
import static org.bytedeco.cuda.global.cudart.*;
import org.bytedeco.cuda.cublas.*;
import static org.bytedeco.cuda.global.cublas.*;
import org.bytedeco.cuda.cudnn.*;
import static org.bytedeco.cuda.global.cudnn.*;
import org.bytedeco.cuda.nvrtc.*;
import static org.bytedeco.cuda.global.nvrtc.*;

import static org.bytedeco.tensorrt.global.nvinfer.*;


/**
 *  \class IRefitter
 * 
 *  \brief Updates weights in an engine.
 * 
 *  \warning Do not inherit from this class, as doing so will break forward-compatibility of the API and ABI.
 *  */
@Namespace("nvinfer1") @Properties(inherit = org.bytedeco.tensorrt.presets.nvinfer.class)
public class IRefitter extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public IRefitter(Pointer p) { super(p); }

    /**
     *  \brief Specify new weights for a layer of given name.
     *  Returns true on success, or false if new weights are rejected.
     *  Possible reasons for rejection are:
     * 
     *  * There is no such layer by that name.
     *  * The layer does not have weights with the specified role.
     *  * The number of weights is inconsistent with the layer’s original specification.
     * 
     *  Modifying the weights before method refit() completes will result in undefined behavior. */
    
    
    //!
    //!
    //!
    public native @Cast("bool") boolean setWeights(String layerName, WeightsRole role, @ByVal Weights weights);
    public native @Cast("bool") boolean setWeights(@Cast("const char*") BytePointer layerName, @Cast("nvinfer1::WeightsRole") int role, @ByVal Weights weights);

    /**
     *  \brief Updates associated engine.  Return true if successful.
     * 
     *  Failure occurs if getMissing() != 0 before the call.
     *  */
    
    
    //!
    //!
    //!
    //!
    //!
    //!
    public native @Cast("bool") boolean refitCudaEngine();

    /**
     *  \brief Get description of missing weights.
     * 
     *  For example, if some Weights have been set, but the engine was optimized
     *  in a way that combines weights, any unsupplied Weights in the combination
     *  are considered missing.
     * 
     *  @param size The number of items that can be safely written to a non-null layerNames or roles.
     *  @param layerNames Where to write the layer names.
     *  @param roles Where to write the weights roles.
     * 
     *  @return The number of missing Weights.
     * 
     *  If layerNames!=nullptr, each written pointer points to a string owned by
     *  the engine being refitted, and becomes invalid when the engine is destroyed.
     *  */
    
    
    //!
    //!
    //!
    //!
    //!
    public native int getMissing(int size, @Cast("const char**") PointerPointer layerNames, @Cast("nvinfer1::WeightsRole*") IntPointer roles);
    public native int getMissing(int size, @Cast("const char**") @ByPtrPtr BytePointer layerNames, @Cast("nvinfer1::WeightsRole*") IntPointer roles);
    public native int getMissing(int size, @Cast("const char**") @ByPtrPtr ByteBuffer layerNames, @Cast("nvinfer1::WeightsRole*") IntBuffer roles);
    public native int getMissing(int size, @Cast("const char**") @ByPtrPtr byte[] layerNames, @Cast("nvinfer1::WeightsRole*") int[] roles);

    /**
     *  \brief Get description of all weights that could be refit.
     * 
     *  @param size The number of items that can be safely written to a non-null layerNames or roles.
     *  @param layerNames Where to write the layer names.
     *  @param roles Where to write the weights roles.
     * 
     *  @return The number of Weights that could be refit.
     * 
     *  If layerNames!=nullptr, each written pointer points to a string owned by
     *  the engine being refitted, and becomes invalid when the engine is destroyed.
     *  */
    public native int getAll(int size, @Cast("const char**") PointerPointer layerNames, @Cast("nvinfer1::WeightsRole*") IntPointer roles);
    public native int getAll(int size, @Cast("const char**") @ByPtrPtr BytePointer layerNames, @Cast("nvinfer1::WeightsRole*") IntPointer roles);
    public native int getAll(int size, @Cast("const char**") @ByPtrPtr ByteBuffer layerNames, @Cast("nvinfer1::WeightsRole*") IntBuffer roles);
    public native int getAll(int size, @Cast("const char**") @ByPtrPtr byte[] layerNames, @Cast("nvinfer1::WeightsRole*") int[] roles);

    public native void destroy();
    /**
     *  Update dynamic range for a tensor.
     * 
     *  @param tensorName The name of an ITensor in the network.
     *  @param min The minimum of the dynamic range for the tensor.
     *  @param max The maximum of the dynamic range for the tensor.
     * 
     *  @return True if successful; false otherwise.
     * 
     *  Returns false if there is no Int8 engine tensor derived from
     *  a network tensor of that name.  If successful, then getMissing
     *  may report that some weights need to be supplied. */
    
    
    //!
    //!
    //!
    //!
    public native @Cast("bool") boolean setDynamicRange(String tensorName, float min, float max);
    public native @Cast("bool") boolean setDynamicRange(@Cast("const char*") BytePointer tensorName, float min, float max);

    /**
     *  \brief Get minimum of dynamic range.
     * 
     *  @return Minimum of dynamic range.
     * 
     *  If the dynamic range was never set, returns the minimum computed during calibration.
     *  */
    
    
    //!
    //!
    //!
    //!
    public native float getDynamicRangeMin(String tensorName);
    public native float getDynamicRangeMin(@Cast("const char*") BytePointer tensorName);

    /**
     *  \brief Get maximum of dynamic range.
     * 
     *  @return Maximum of dynamic range.
     * 
     *  If the dynamic range was never set, returns the maximum computed during calibration.
     *  */
    
    
    //!
    //!
    //!
    //!
    //!
    public native float getDynamicRangeMax(String tensorName);
    public native float getDynamicRangeMax(@Cast("const char*") BytePointer tensorName);

    /**
     *  \brief Get names of all tensors that have refittable dynamic ranges.
     * 
     *  @param size The number of items that can be safely written to a non-null tensorNames.
     *  @param tensorNames Where to write the layer names.
     * 
     *  @return The number of Weights that could be refit.
     * 
     *  If tensorNames!=nullptr, each written pointer points to a string owned by
     *  the engine being refitted, and becomes invalid when the engine is destroyed.
     *  */
    
    
    //!
    //!
    //!
    //!
    public native int getTensorsWithDynamicRange(int size, @Cast("const char**") PointerPointer tensorNames);
    public native int getTensorsWithDynamicRange(int size, @Cast("const char**") @ByPtrPtr BytePointer tensorNames);
    public native int getTensorsWithDynamicRange(int size, @Cast("const char**") @ByPtrPtr ByteBuffer tensorNames);
    public native int getTensorsWithDynamicRange(int size, @Cast("const char**") @ByPtrPtr byte[] tensorNames);

    /**
     *  \brief Set the ErrorRecorder for this interface
     * 
     *  Assigns the ErrorRecorder to this interface. The ErrorRecorder will track all errors during execution.
     *  This function will call incRefCount of the registered ErrorRecorder at least once. Setting
     *  recorder to nullptr unregisters the recorder with the interface, resulting in a call to decRefCount if
     *  a recorder has been registered.
     * 
     *  @param recorder The error recorder to register with this interface. */
    //
    /** @see getErrorRecorder
    /** */
    
    
    //!
    //!
    //!
    //!
    //!
    public native void setErrorRecorder(IErrorRecorder recorder);

    /**
     *  \brief get the ErrorRecorder assigned to this interface.
     * 
     *  Retrieves the assigned error recorder object for the given class. A default error recorder does not exist,
     *  so a nullptr will be returned if setErrorRecorder has not been called.
     * 
     *  @return A pointer to the IErrorRecorder object that has been registered.
     * 
     *  @see setErrorRecorder
     *  */
    public native IErrorRecorder getErrorRecorder();
}
