package org.bytedeco.pytorch.dataframe.enums;

/** Plot kinds for plotting façades. */
public enum PlotType {
    LINE, BAR, SCATTER, HISTOGRAM, HEATMAP, BOX, PIE, AREA, VIOLIN, BUBBLE, RADAR, FUNNEL
}
