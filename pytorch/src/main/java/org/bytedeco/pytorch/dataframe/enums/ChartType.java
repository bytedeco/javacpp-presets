package org.bytedeco.pytorch.dataframe.enums;

/** Chart kinds for plotting façades. */
public enum ChartType {
    LINE, BAR, SCATTER, HEATMAP, AREA, PIE, BUBBLE, RADAR, SANKEY, FUNNEL, VIOLIN, BOX, HISTOGRAM
}
