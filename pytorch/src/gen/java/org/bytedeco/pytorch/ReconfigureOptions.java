// Targeted by JavaCPP version 1.5.15-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.Function;
import org.bytedeco.pytorch.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;


@Namespace("c10d") @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class ReconfigureOptions extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public ReconfigureOptions() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public ReconfigureOptions(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ReconfigureOptions(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public ReconfigureOptions position(long position) {
        return (ReconfigureOptions)super.position(position);
    }
    @Override public ReconfigureOptions getPointer(long i) {
        return new ReconfigureOptions((Pointer)this).offsetAddress(i);
    }

  // Uniquely identifies this instance of the communicator. Must not have been
  // used previously on this communicator; pass a fresh value on every
  // (re)initialization.
  public native @Cast("int64_t") long uuid(); public native ReconfigureOptions uuid(long setter);

  // Members participating in the communicator, one handle per rank. A vector
  // assigns ranks by position (ordered); an unordered_set lets the backend
  // choose the rank assignment.
  public native @ByRef StringHandles handles(); public native ReconfigureOptions handles(StringHandles setter);

  // How long to allow reconfiguration before failing. nullopt uses the
  // backend's default timeout.
  public native @Optional Milliseconds timeout(); public native ReconfigureOptions timeout(Milliseconds setter);

  // Backend-specific configuration key-value pairs.
  public native @ByRef ExtraFilesMap hints(); public native ReconfigureOptions hints(ExtraFilesMap setter);
}
