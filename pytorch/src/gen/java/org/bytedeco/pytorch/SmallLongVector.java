// Targeted by JavaCPP version 1.5.15-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.Function;
import org.bytedeco.pytorch.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;


/** This is a 'vector' (really, a variable-sized array), optimized
 *  for the case when the array is small.  It contains some number of elements
 *  in-place, which allows it to avoid heap allocation when the actual number of
 *  elements is below that threshold.  This allows normal "small" cases to be
 *  fast without losing generality for large inputs.
 * 
 *  \note
 *  In the absence of a well-motivated choice for the number of inlined
 *  elements \p N, it is recommended to use \c SmallVector<T> (that is,
 *  omitting the \p N). This will choose a default number of inlined elements
 *  reasonable for allocation on the stack (for example, trying to keep \c
 *  sizeof(SmallVector<T>) around 64 bytes).
 * 
 *  \warning This does not attempt to be exception safe.
 * 
 *  @see https://llvm.org/docs/ProgrammersManual.html#llvm-adt-smallvector-h */
@Name("c10::SmallVector<int64_t,5>") @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class SmallLongVector extends LongSmallVectorImpl {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public SmallLongVector(Pointer p) { super(p); }

  public SmallLongVector() { super((Pointer)null); allocate(); }
  private native void allocate();

  public SmallLongVector(@Cast("size_t") long Size, @Cast("const int64_t") long Value/*=int64_t()*/) { super((Pointer)null); allocate(Size, Value); }
  private native void allocate(@Cast("size_t") long Size, @Cast("const int64_t") long Value/*=int64_t()*/);
  public SmallLongVector(@Cast("size_t") long Size) { super((Pointer)null); allocate(Size); }
  private native void allocate(@Cast("size_t") long Size);

  // note: The enable_if restricts Container to types that have a .begin() and
  // .end() that return valid input iterators.

  public SmallLongVector(@Cast("const c10::SmallVector<int64_t,5>*") @ByRef SmallLongVector RHS) { super((Pointer)null); allocate(RHS); }
  private native void allocate(@Cast("const c10::SmallVector<int64_t,5>*") @ByRef SmallLongVector RHS);

  public native @Cast("c10::SmallVector<int64_t,5>*") @ByRef @Name("operator =") SmallLongVector put(@Cast("const c10::SmallVector<int64_t,5>*") @ByRef SmallLongVector RHS);

  // note: The enable_if restricts Container to types that have a .begin() and
  // .end() that return valid input iterators.

  public SmallLongVector(@ByRef(true) LongSmallVectorImpl RHS) { super((Pointer)null); allocate(RHS); }
  @NoException(true) private native void allocate(@ByRef(true) LongSmallVectorImpl RHS);

  public native @Cast("c10::SmallVector<int64_t,5>*") @ByRef @Name("operator =") @NoException(true) SmallLongVector put(@ByRef(true) LongSmallVectorImpl RHS);

  // note: The enable_if restricts Container to types that have a .begin() and
  // .end() that return valid input iterators.
}
