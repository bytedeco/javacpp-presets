// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch;
import org.bytedeco.pytorch.jit.*;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.jit.Function;
import org.bytedeco.pytorch.nn.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;

@Name("std::vector<std::vector<uint8_t> >") @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class ByteVectorVector extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ByteVectorVector(Pointer p) { super(p); }
    public ByteVectorVector(ByteVector value) { this(1); put(0, value); }
    public ByteVectorVector(ByteVector ... array) { this(array.length); put(array); }
    public ByteVectorVector()       { allocate();  }
    public ByteVectorVector(long n) { allocate(n); }
    private native void allocate();
    private native void allocate(@Cast("size_t") long n);
    public native @Name("operator =") @ByRef ByteVectorVector put(@ByRef ByteVectorVector x);

    public boolean empty() { return size() == 0; }
    public native long size();
    public void clear() { resize(0); }
    public native void resize(@Cast("size_t") long n);

    public ByteVector front() { return get(0); }
    public ByteVector back() { return get(size() - 1); }
    @Index(function = "at") public native @Cast("std::vector<uint8_t>*") @ByRef ByteVector get(@Cast("size_t") long i);
    public native ByteVectorVector put(@Cast("size_t") long i, ByteVector value);

    public native @ByVal Iterator insert(@ByVal Iterator pos, @Cast("std::vector<uint8_t>*") @ByRef ByteVector value);
    public native @ByVal Iterator erase(@ByVal Iterator pos);
    public native @ByVal Iterator begin();
    public native @ByVal Iterator end();
    @NoOffset @Name("iterator") public static class Iterator extends Pointer {
        public Iterator(Pointer p) { super(p); }
        public Iterator() { }

        public native @Name("operator ++") @ByRef Iterator increment();
        public native @Name("operator ==") boolean equals(@ByRef Iterator it);
        public native @Name("operator *") @Cast("std::vector<uint8_t>*") @ByRef @Const ByteVector get();
    }

    public ByteVector[] get() {
        ByteVector[] array = new ByteVector[size() < Integer.MAX_VALUE ? (int)size() : Integer.MAX_VALUE];
        for (int i = 0; i < array.length; i++) {
            array[i] = get(i);
        }
        return array;
    }
    @Override public String toString() {
        return java.util.Arrays.toString(get());
    }

    public ByteVector pop_back() {
        long size = size();
        ByteVector value = get(size - 1);
        resize(size - 1);
        return value;
    }
    public ByteVectorVector push_back(ByteVector value) {
        long size = size();
        resize(size + 1);
        return put(size, value);
    }
    public ByteVectorVector put(ByteVector value) {
        if (size() != 1) { resize(1); }
        return put(0, value);
    }
    public ByteVectorVector put(ByteVector ... array) {
        if (size() != array.length) { resize(array.length); }
        for (int i = 0; i < array.length; i++) {
            put(i, array[i]);
        }
        return this;
    }
}

