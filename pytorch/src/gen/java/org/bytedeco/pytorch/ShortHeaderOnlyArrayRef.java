// Targeted by JavaCPP version 1.5.13-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.Function;
import org.bytedeco.pytorch.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;

@Name("c10::HeaderOnlyArrayRef<jshort>") @NoOffset @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class ShortHeaderOnlyArrayRef extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ShortHeaderOnlyArrayRef(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public ShortHeaderOnlyArrayRef(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public ShortHeaderOnlyArrayRef position(long position) {
        return (ShortHeaderOnlyArrayRef)super.position(position);
    }
    @Override public ShortHeaderOnlyArrayRef getPointer(long i) {
        return new ShortHeaderOnlyArrayRef((Pointer)this).offsetAddress(i);
    }

  /** \name Constructors
   *  \{
   <p>
   *  Construct an empty HeaderOnlyArrayRef. */
  /* implicit */ public ShortHeaderOnlyArrayRef() { super((Pointer)null); allocate(); }
private native void allocate();

  /** Construct a HeaderOnlyArrayRef from a single element. */
  // TODO Make this explicit
  

  /** Construct a HeaderOnlyArrayRef from a pointer and length. */
  public ShortHeaderOnlyArrayRef(@Const ShortPointer data, @Cast("size_t") long length) { super((Pointer)null); allocate(data, length); }
  private native void allocate(@Const ShortPointer data, @Cast("size_t") long length);
  public ShortHeaderOnlyArrayRef(@Const ShortBuffer data, @Cast("size_t") long length) { super((Pointer)null); allocate(data, length); }
  private native void allocate(@Const ShortBuffer data, @Cast("size_t") long length);
  public ShortHeaderOnlyArrayRef(@Const short[] data, @Cast("size_t") long length) { super((Pointer)null); allocate(data, length); }
  private native void allocate(@Const short[] data, @Cast("size_t") long length);

  /** Construct a HeaderOnlyArrayRef from a range. */
  public ShortHeaderOnlyArrayRef(@Const ShortPointer begin, @Const ShortPointer end) { super((Pointer)null); allocate(begin, end); }
  private native void allocate(@Const ShortPointer begin, @Const ShortPointer end);
  public ShortHeaderOnlyArrayRef(@Const ShortBuffer begin, @Const ShortBuffer end) { super((Pointer)null); allocate(begin, end); }
  private native void allocate(@Const ShortBuffer begin, @Const ShortBuffer end);
  public ShortHeaderOnlyArrayRef(@Const short[] begin, @Const short[] end) { super((Pointer)null); allocate(begin, end); }
  private native void allocate(@Const short[] begin, @Const short[] end);

  /** Construct a HeaderOnlyArrayRef from a std::vector. */
  // The enable_if stuff here makes sure that this isn't used for
  // std::vector<bool>, because ArrayRef can't work on a std::vector<bool>
  // bitfield.

  /** Construct a HeaderOnlyArrayRef from a std::array */

  /** Construct a HeaderOnlyArrayRef from a C array. */

  /** Construct a HeaderOnlyArrayRef from a std::initializer_list. */
  /* implicit */

  /** \}
   *  \name Simple Operations
   *  \{ */

  
  

  // These are actually the same as iterator, since ArrayRef only
  // gives you const iterators.
  
  

  /** Check if all elements in the array satisfy the given expression */
  

  /** empty - Check if the array is empty. */
  public native @Cast("const bool") boolean empty();

  public native @Const ShortPointer data();

  /** size - Get the array size. */
  public native @Cast("const size_t") long size();

  /** front - Get the first element. */
  public native short front();

  /** back - Get the last element. */
  public native short back();

  /** equals - Check for element-wise equality. */
  

  /** slice(n, m) - Take M elements of the array starting at element N */
  public native @Const @ByVal ShortHeaderOnlyArrayRef slice(@Cast("size_t") long N, @Cast("size_t") long M);

  /** slice(n) - Chop off the first N elements of the array. */
  public native @Const @ByVal ShortHeaderOnlyArrayRef slice(@Cast("size_t") long N);

  /** \}
   *  \name Operator Overloads
   *  \{ */
  public native @Name("operator []") short get(@Cast("size_t") long Index);

  /** Vector compatibility */
  
  ///
  public native short at(@Cast("size_t") long Index);

  /** Disallow accidental assignment from a temporary.
   * 
   *  The declaration here is extra complicated so that "arrayRef = {}"
   *  continues to select the move assignment operator. */
  

  /** Disallow accidental assignment from a temporary.
   * 
   *  The declaration here is extra complicated so that "arrayRef = {}"
   *  continues to select the move assignment operator. */
  

  /** \}
   *  \name Expensive Operations
   *  \{ */
  public native @StdVector ShortPointer vec();

  /** \} */
}
