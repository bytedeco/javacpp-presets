// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch.quantizer;

import org.bytedeco.pytorch.jit.*;

import org.bytedeco.pytorch.nn.*;

import org.bytedeco.pytorch.*;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.jit.Function;
import org.bytedeco.pytorch.nn.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;


/**
 * PerChannelAffineFloatQParamsQuantizer is the same as PerChannelAffineQuantizer
 * except that it expects both scale and zero point to be floating point values.
 *
 * This quantizer uses the kPerChannelAffineFloatQParams qscheme which is a variant of
 * kPerChannelAffine.
 *
 * The quantize equation in this case looks like -
 * Xq = (Xf - zero_point) * inv_scale, where inv_scale = 1.0/scale
 *
 * Note: Usage of floating point zero point is useful in cases where 0 doesn't need to
 * be exactly represented in the quantized space. We can get additional precision by
 * using floating point values for zero point.
 */
@Namespace("at") @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class PerChannelAffineFloatQParamsQuantizer extends PerChannelAffineQuantizer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public PerChannelAffineFloatQParamsQuantizer(Pointer p) { super(p); }

  public PerChannelAffineFloatQParamsQuantizer(
        ScalarType scalar_type,
        @ByVal Tensor scales,
        @ByVal Tensor zero_points,
        @Cast("int64_t") long axis) { super((Pointer)null); allocate(scalar_type, scales, zero_points, axis); }
  private native void allocate(
        ScalarType scalar_type,
        @ByVal Tensor scales,
        @ByVal Tensor zero_points,
        @Cast("int64_t") long axis);

  public native QScheme qscheme();

  public native @ByVal Tensor quantize(@Const @ByRef Tensor tensor);
  public native @ByVal Tensor dequantize(@Const @ByRef Tensor qtensor);
  public native @ByRef Tensor dequantize_out(@ByRef Tensor rtensor, @Const @ByRef Tensor qtensor);

  public native @Cast("bool") boolean equalTo(@IntrusivePtr("at::Quantizer") @Cast({"", "c10::intrusive_ptr<at::Quantizer>&"}) Quantizer other);
}
