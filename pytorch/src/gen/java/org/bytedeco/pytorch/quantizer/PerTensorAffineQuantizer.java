// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch.quantizer;

import org.bytedeco.pytorch.jit.*;

import org.bytedeco.pytorch.nn.*;

import org.bytedeco.pytorch.*;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.jit.Function;
import org.bytedeco.pytorch.nn.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;


// Note that we will not have Symmetric Quantizer in backend to reduce
// complications in quantized kernel implementation.

/**
 * PerTensorAffineQuantizer stores a scale and a zero_point, which is used for
 * all the values in the Tensor.
 */
@Namespace("at") @NoOffset @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class PerTensorAffineQuantizer extends AffineQuantizer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public PerTensorAffineQuantizer(Pointer p) { super(p); }

  public PerTensorAffineQuantizer(ScalarType scalar_type, double scale, @Cast("int64_t") long zero_point) { super((Pointer)null); allocate(scalar_type, scale, zero_point); }
  private native void allocate(ScalarType scalar_type, double scale, @Cast("int64_t") long zero_point);

  public native @ByVal Tensor quantize(@Const @ByRef Tensor tensor);
  public native @ByVal Tensor dequantize(@Const @ByRef Tensor qtensor);
  public native @ByRef Tensor dequantize_out(@ByRef Tensor rtensor, @Const @ByRef Tensor qtensor);

  public native QScheme qscheme();

  public native double scale();

  public native @Cast("int64_t") long zero_point();

  public native @Cast("bool") boolean equalTo(@IntrusivePtr("at::Quantizer") @Cast({"", "c10::intrusive_ptr<at::Quantizer>&"}) Quantizer other);
}
