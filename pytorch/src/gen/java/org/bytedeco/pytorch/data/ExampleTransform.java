// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch.data;

import org.bytedeco.pytorch.jit.*;

import org.bytedeco.pytorch.nn.*;

import org.bytedeco.pytorch.*;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.jit.Function;
import org.bytedeco.pytorch.nn.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;


/** A transformation of individual input examples to individual output examples.
 * 
 *  Just like a {@code Dataset} is a {@code BatchDataset}, a {@code Transform} is a
 *  {@code BatchTransform} that can operate on the level of individual examples rather
 *  than entire batches. The batch-level transform is implemented (by default)
 *  in terms of the example-level transform, though this can be customized. */
@Name("torch::data::transforms::Transform<torch::data::Example<torch::Tensor,torch::Tensor>,torch::data::Example<torch::Tensor,torch::Tensor> >") @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class ExampleTransform extends ExampleBatchTransform {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ExampleTransform(Pointer p) { super(p); }


  /** Applies the transformation to the given {@code input}. */
  @Virtual(true) public native @ByVal @Cast("torch::data::transforms::Transform<torch::data::Example<torch::Tensor,torch::Tensor>,torch::data::Example<torch::Tensor,torch::Tensor> >::OutputType*") Example apply(@ByVal @Cast("torch::data::transforms::Transform<torch::data::Example<torch::Tensor,torch::Tensor>,torch::data::Example<torch::Tensor,torch::Tensor> >::InputType*") Example input);

  /** Applies the {@code transformation} over the entire {@code input_batch}. */
  @Virtual public native @ByVal ExampleVector apply_batch(@ByVal ExampleVector input_batch);
}
