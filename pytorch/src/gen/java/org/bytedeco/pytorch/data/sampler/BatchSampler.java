// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch.data.sampler;

import org.bytedeco.pytorch.serialize.*;

import org.bytedeco.pytorch.jit.*;

import org.bytedeco.pytorch.nn.*;

import org.bytedeco.pytorch.data.*;

import org.bytedeco.pytorch.*;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.jit.Function;
import org.bytedeco.pytorch.nn.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;


// ============================================================================
// BatchSampler — drop_last wrapper around another Sampler<>
// ============================================================================

/** Wraps another {<pre>{@code Sampler<>} and optionally drops the final incomplete
 *  batch ({@code drop_last=true}), matching Python
 *  {@code torch.utils.data.BatchSampler} drop_last semantics on top of the C++
 *  {@code next(batch_size)} batching model.
 * 
 *  Ownership: holds a {@code shared_ptr} to the inner sampler so Java can keep
 *  both peers alive. */
@Namespace("torch::data::samplers") @NoOffset @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class BatchSampler extends Sampler {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public BatchSampler(Pointer p) { super(p); }

  public BatchSampler(@SharedPtr @ByVal Sampler sampler, @Cast("bool") boolean drop_last) { super((Pointer)null); allocate(sampler, drop_last); }
  private native void allocate(@SharedPtr @ByVal Sampler sampler, @Cast("bool") boolean drop_last);

  public native void reset(@ByVal(nullValue = "std::optional<size_t>(std::nullopt)") SizeTOptional new_size);
  public native void reset();

  public native @ByVal SizeTVectorOptional next(@Cast("size_t") long batch_size);

  public native void save(@ByRef OutputArchive archive);

  public native void load(@ByRef InputArchive archive);

  public native @ByRef Sampler sampler();

  public native @Cast("bool") @NoException(true) boolean drop_last();
}
