// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch.optim.schedulers;

import org.bytedeco.pytorch.optim.*;

import org.bytedeco.pytorch.jit.*;

import org.bytedeco.pytorch.nn.*;

import org.bytedeco.pytorch.*;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.jit.Function;
import org.bytedeco.pytorch.nn.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;


// ============================================================================
// CyclicLR (triangular mode only by default; scale_fn optional via mode string)
// ============================================================================

@Namespace("torch::optim") @NoOffset @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class CyclicLR extends LRScheduler {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CyclicLR(Pointer p) { super(p); }

  public CyclicLR(
        @ByRef Optimizer optimizer,
        double base_lr,
        double max_lr,
        @Cast("unsigned") int step_size_up/*=2000*/,
        @Cast("unsigned") int step_size_down/*=0*/,
        @StdString BytePointer mode/*="triangular"*/,
        double gamma/*=1.0*/) { super((Pointer)null); allocate(optimizer, base_lr, max_lr, step_size_up, step_size_down, mode, gamma); }
  private native void allocate(
        @ByRef Optimizer optimizer,
        double base_lr,
        double max_lr,
        @Cast("unsigned") int step_size_up/*=2000*/,
        @Cast("unsigned") int step_size_down/*=0*/,
        @StdString BytePointer mode/*="triangular"*/,
        double gamma/*=1.0*/);
  public CyclicLR(
        @ByRef Optimizer optimizer,
        double base_lr,
        double max_lr) { super((Pointer)null); allocate(optimizer, base_lr, max_lr); }
  private native void allocate(
        @ByRef Optimizer optimizer,
        double base_lr,
        double max_lr);
  public CyclicLR(
        @ByRef Optimizer optimizer,
        double base_lr,
        double max_lr,
        @Cast("unsigned") int step_size_up/*=2000*/,
        @Cast("unsigned") int step_size_down/*=0*/,
        @StdString String mode/*="triangular"*/,
        double gamma/*=1.0*/) { super((Pointer)null); allocate(optimizer, base_lr, max_lr, step_size_up, step_size_down, mode, gamma); }
  private native void allocate(
        @ByRef Optimizer optimizer,
        double base_lr,
        double max_lr,
        @Cast("unsigned") int step_size_up/*=2000*/,
        @Cast("unsigned") int step_size_down/*=0*/,
        @StdString String mode/*="triangular"*/,
        double gamma/*=1.0*/);
}
