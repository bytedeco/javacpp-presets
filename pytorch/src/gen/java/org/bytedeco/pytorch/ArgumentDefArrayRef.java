// Targeted by JavaCPP version 1.5.13-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.Function;
import org.bytedeco.pytorch.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;

@Name("c10::ArrayRef<c10::detail::infer_schema::ArgumentDef>") @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class ArgumentDefArrayRef extends ArgumentDefHeaderOnlyArrayRef {
    static { Loader.load(); }


   public ArgumentDefArrayRef() { super((Pointer)null); allocate(); }
  private native void allocate();
    public ArgumentDefArrayRef(@Const ArgumentDef data, @Cast("size_t") long length) { super((Pointer)null); allocate(data, length); }
    private native void allocate(@Const ArgumentDef data, @Cast("size_t") long length);
    public ArgumentDefArrayRef(@Const ArgumentDef begin, @Const ArgumentDef end) { super((Pointer)null); allocate(begin, end); }
    private native void allocate(@Const ArgumentDef begin, @Const ArgumentDef end);
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ArgumentDefArrayRef(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public ArgumentDefArrayRef(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public ArgumentDefArrayRef position(long position) {
        return (ArgumentDefArrayRef)super.position(position);
    }
    @Override public ArgumentDefArrayRef getPointer(long i) {
        return new ArgumentDefArrayRef((Pointer)this).offsetAddress(i);
    }

  /** \name Constructors, all inherited from HeaderOnlyArrayRef except for
   *  SmallVector. As inherited constructors won't work with class template
   *  argument deduction (CTAD) until C++23, we add deduction guides after
   *  the class definition to enable CTAD.
   *  \{ */

  /** Construct an ArrayRef from a SmallVector. This is templated in order to
   *  avoid instantiating SmallVectorTemplateCommon<T> whenever we
   *  copy-construct an ArrayRef.
   *  NOTE: this is the only constructor that is not inherited from
   *  HeaderOnlyArrayRef. */

  /** \}
   *  \name Simple Operations, mostly inherited from HeaderOnlyArrayRef
   *  \{
   <p>
   *  front - Get the first element.
   *  We deviate from HeaderOnlyArrayRef by using TORCH_CHECK instead of
   *  STD_TORCH_CHECK */
  public native @Const @ByRef ArgumentDef front();

  /** back - Get the last element.
   *  We deviate from HeaderOnlyArrayRef by using TORCH_CHECK instead of
   *  STD_TORCH_CHECK */
  public native @Const @ByRef ArgumentDef back();

  /** slice(n, m) - Take M elements of the array starting at element N
   *  We deviate from HeaderOnlyArrayRef by using TORCH_CHECK instead of
   *  STD_TORCH_CHECK */
  public native @Const @ByVal ArgumentDefArrayRef slice(@Cast("size_t") long N, @Cast("size_t") long M);

  /** slice(n) - Chop off the first N elements of the array.
   *  We deviate from HeaderOnlyArrayRef by using TORCH_CHECK instead of
   *  STD_TORCH_CHECK */
  public native @Const @ByVal ArgumentDefArrayRef slice(@Cast("size_t") long N);

  /** \}
   *  \name Operator Overloads
   *  \{
   <p>
   *  Vector compatibility
   *  We deviate from HeaderOnlyArrayRef by using TORCH_CHECK instead of
   *  STD_TORCH_CHECK */
  
  ///
  public native @Const @ByRef ArgumentDef at(@Cast("size_t") long Index);

  /** Disallow accidental assignment from a temporary.
   * 
   *  The declaration here is extra complicated so that "arrayRef = {}"
   *  continues to select the move assignment operator. */
  

  /** Disallow accidental assignment from a temporary.
   * 
   *  The declaration here is extra complicated so that "arrayRef = {}"
   *  continues to select the move assignment operator. */
  

  /** \} */
}
