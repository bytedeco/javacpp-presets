// Targeted by JavaCPP version 1.5.11-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.Function;
import org.bytedeco.pytorch.functions.*;
import org.bytedeco.pytorch.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;

import static org.bytedeco.pytorch.global.torch.*;


@Namespace("torch::inductor") @NoOffset @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class AOTIModelContainerRunner extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public AOTIModelContainerRunner(Pointer p) { super(p); }

  
  
  
  
  

  public native @Cast({"", "std::vector<torch::Tensor>"}) @StdMove TensorVector run(
        @Cast({"", "std::vector<torch::Tensor>"}) @StdMove TensorVector inputs);

  public native @ByVal BytePointerVector get_call_spec();

  public AOTIModelContainerRunner(
        @Cast("const char*") BytePointer model_path,
        @Cast("size_t") long num_models,
        @Cast("bool") boolean is_cpu,
        @Cast("const char*") BytePointer cubin_dir) { super((Pointer)null); allocate(model_path, num_models, is_cpu, cubin_dir); }
  private native void allocate(
        @Cast("const char*") BytePointer model_path,
        @Cast("size_t") long num_models,
        @Cast("bool") boolean is_cpu,
        @Cast("const char*") BytePointer cubin_dir);
  public AOTIModelContainerRunner(
        String model_path,
        @Cast("size_t") long num_models,
        @Cast("bool") boolean is_cpu,
        String cubin_dir) { super((Pointer)null); allocate(model_path, num_models, is_cpu, cubin_dir); }
  private native void allocate(
        String model_path,
        @Cast("size_t") long num_models,
        @Cast("bool") boolean is_cpu,
        String cubin_dir);
}
