// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch;
import org.bytedeco.pytorch.jit.*;

import org.bytedeco.pytorch.Allocator;
import org.bytedeco.pytorch.jit.Function;
import org.bytedeco.pytorch.nn.Module;
import org.bytedeco.javacpp.annotation.Cast;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;

import static org.bytedeco.pytorch.global.torch.*;

@Name("std::vector<std::vector<std::vector<std::vector<uint8_t> > > >") @Properties(inherit = org.bytedeco.pytorch.presets.torch.class)
public class ByteVectorVectorVectorVector extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ByteVectorVectorVectorVector(Pointer p) { super(p); }
    public ByteVectorVectorVectorVector(ByteVectorVectorVector value) { this(1); put(0, value); }
    public ByteVectorVectorVectorVector(ByteVectorVectorVector ... array) { this(array.length); put(array); }
    public ByteVectorVectorVectorVector()       { allocate();  }
    public ByteVectorVectorVectorVector(long n) { allocate(n); }
    private native void allocate();
    private native void allocate(@Cast("size_t") long n);
    public native @Name("operator =") @ByRef ByteVectorVectorVectorVector put(@ByRef ByteVectorVectorVectorVector x);

    public boolean empty() { return size() == 0; }
    public native long size();
    public void clear() { resize(0); }
    public native void resize(@Cast("size_t") long n);

    public ByteVectorVectorVector front() { return get(0); }
    public ByteVectorVectorVector back() { return get(size() - 1); }
    @Index(function = "at") public native @Cast("std::vector<std::vector<std::vector<uint8_t> > >*") @ByRef ByteVectorVectorVector get(@Cast("size_t") long i);
    public native ByteVectorVectorVectorVector put(@Cast("size_t") long i, ByteVectorVectorVector value);

    public native @ByVal Iterator insert(@ByVal Iterator pos, @Cast("std::vector<std::vector<std::vector<uint8_t> > >*") @ByRef ByteVectorVectorVector value);
    public native @ByVal Iterator erase(@ByVal Iterator pos);
    public native @ByVal Iterator begin();
    public native @ByVal Iterator end();
    @NoOffset @Name("iterator") public static class Iterator extends Pointer {
        public Iterator(Pointer p) { super(p); }
        public Iterator() { }

        public native @Name("operator ++") @ByRef Iterator increment();
        public native @Name("operator ==") boolean equals(@ByRef Iterator it);
        public native @Name("operator *") @Cast("std::vector<std::vector<std::vector<uint8_t> > >*") @ByRef @Const ByteVectorVectorVector get();
    }

    public ByteVectorVectorVector[] get() {
        ByteVectorVectorVector[] array = new ByteVectorVectorVector[size() < Integer.MAX_VALUE ? (int)size() : Integer.MAX_VALUE];
        for (int i = 0; i < array.length; i++) {
            array[i] = get(i);
        }
        return array;
    }
    @Override public String toString() {
        return java.util.Arrays.toString(get());
    }

    public ByteVectorVectorVector pop_back() {
        long size = size();
        ByteVectorVectorVector value = get(size - 1);
        resize(size - 1);
        return value;
    }
    public ByteVectorVectorVectorVector push_back(ByteVectorVectorVector value) {
        long size = size();
        resize(size + 1);
        return put(size, value);
    }
    public ByteVectorVectorVectorVector put(ByteVectorVectorVector value) {
        if (size() != 1) { resize(1); }
        return put(0, value);
    }
    public ByteVectorVectorVectorVector put(ByteVectorVectorVector ... array) {
        if (size() != array.length) { resize(array.length); }
        for (int i = 0; i < array.length; i++) {
            put(i, array[i]);
        }
        return this;
    }
}

