// Targeted by JavaCPP version 1.5.13-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch.gloo;

import org.bytedeco.pytorch.gloo.Device;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;
import org.bytedeco.pytorch.*;
import static org.bytedeco.pytorch.global.torch.*;

import static org.bytedeco.pytorch.global.torch_gloo.*;


@Namespace("gloo::transport::tcp") @NoOffset @Properties(inherit = org.bytedeco.pytorch.presets.torch_gloo.class)
public class Loop extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Loop(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public Loop(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public Loop position(long position) {
        return (Loop)super.position(position);
    }
    @Override public Loop getPointer(long i) {
        return new Loop((Pointer)this).offsetAddress(i);
    }

  public Loop() { super((Pointer)null); allocate(); }
  private native void allocate();

  // WARNING: These require the caller to ensure that the handler
  // outlives the loop or is unregistered before destruction.
  public native void registerDescriptor(int fd, int events, Handler h);
  // If a shared_ptr<Handler> is used, the pointer will be released either
  // when the handler is unregistered or a new handler is registered.

  public native void unregisterDescriptor(int fd, Handler h);

  

  public native void run();

  public native void shutdown();
}
