// Targeted by JavaCPP version 1.5.13-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.pytorch.gloo;

import org.bytedeco.pytorch.gloo.Device;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import static org.bytedeco.openblas.global.openblas_nolapack.*;
import static org.bytedeco.openblas.global.openblas.*;
import org.bytedeco.javacpp.chrono.*;
import static org.bytedeco.javacpp.global.chrono.*;
import org.bytedeco.pytorch.*;
import static org.bytedeco.pytorch.global.torch.*;

import static org.bytedeco.pytorch.global.gloo.*;


@Namespace("gloo::transport::tcp") @NoOffset @Properties(inherit = org.bytedeco.pytorch.presets.gloo.class)
public class Socket extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Socket(Pointer p) { super(p); }

  public static native @SharedPtr Socket createForFamily(@Cast("sa_family_t") long ai_family);

  public Socket(int fd) { super((Pointer)null); allocate(fd); }
  private native void allocate(int fd);

  // Return underlying file descriptor.
  public native int fd();

  // Release underlying file descriptor.
  public native int release();

  // Enable or disable SO_REUSEADDR socket option.
  public native void reuseAddr(@Cast("bool") boolean on);

  // Enable or disable TCP_NODELAY socket option.
  public native void noDelay(@Cast("bool") boolean on);

  // Configure if the socket is blocking or not.
  public native void block(@Cast("bool") boolean on);

  // Configure recv timeout.
  public native void recvTimeout(@ByVal Milliseconds timeout);

  // Configure send timeout.
  public native void sendTimeout(@ByVal Milliseconds timeout);

  // Bind socket to address.
  public native void bind(@Cast("const sockaddr_storage*") @ByRef Pointer ss);

  // Bind socket to address.
  public native void bind(@Cast("const sockaddr*") Pointer addr, @Cast("socklen_t") long addrlen);

  // Listen on socket.
  public native void listen(int backlog);

  // Accept new socket connecting to listening socket.
  public native @SharedPtr Socket accept();

  // Connect to address.
  public native void connect(@Cast("const sockaddr_storage*") @ByRef Pointer ss);

  // Connect to address.
  public native void connect(@Cast("const sockaddr*") Pointer addr, @Cast("socklen_t") long addrlen);

  // Proxy to read(2) with EINTR retry.
  public native @Cast("ssize_t") long read(Pointer buf, @Cast("size_t") long count);

  // Proxy to write(2) with EINTR retry.
  public native @Cast("ssize_t") long write(@Const Pointer buf, @Cast("size_t") long count);

  // Return address for getsockname(2).
  

  // Return address for getpeername(2).
  

  // Return address for getpeername(2) if possible, else an empty Address.
  
}
