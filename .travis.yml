dist: trusty
sudo: required
cache:
  directories:
      - $HOME/downloads
addons:
  apt:
    packages:
    - yasm
    - nasm

language: java
services:
      - docker

matrix:
   include:
      - os: linux
        env: PROJ=onnx OS=linux-x86_64
        install: true
        script: ./ci/install-travis.sh
