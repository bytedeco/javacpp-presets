language: cpp
compiler:
- gcc
- clang
before_install:
- echo $LANG
- echo $LC_ALL
- sudo apt-get install -y maven nasm yasm
before_script:
- git clone https://github.com/bytedeco/javacpp
- pushd javacpp; mvn install; popd
script:
- ./cppbuild.sh install opencv ffmpeg
- mvn package --projects .,opencv,ffmpeg
