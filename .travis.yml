dist: xenial
sudo: required
cache:
  directories:
      - $HOME/.m2
      - $HOME/.ccache
      - $HOME/downloads
  timeout: 1000
language: java
services:
      - docker

matrix:
   include:
      - os: linux
        env: PROJ=mkl-dnn OS=linux-x86_64
        install: true
        script: ./ci/install-travis.sh
      - os: osx
        osx_image: xcode8.3
        env: PROJ=mkl-dnn OS=macosx-x86_64
        install: true
        script: ./ci/install-travis.sh
