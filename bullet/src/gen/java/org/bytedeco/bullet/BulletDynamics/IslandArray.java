// Targeted by JavaCPP version 1.5.9-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.bullet.BulletDynamics;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;
import org.bytedeco.bullet.LinearMath.*;
import static org.bytedeco.bullet.global.LinearMath.*;
import org.bytedeco.bullet.BulletCollision.*;
import static org.bytedeco.bullet.global.BulletCollision.*;

import static org.bytedeco.bullet.global.BulletDynamics.*;

@Name("btAlignedObjectArray<btSimulationIslandManagerMt::Island*>") @NoOffset @Properties(inherit = org.bytedeco.bullet.presets.BulletDynamics.class)
public class IslandArray extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public IslandArray(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public IslandArray(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public IslandArray position(long position) {
        return (IslandArray)super.position(position);
    }
    @Override public IslandArray getPointer(long i) {
        return new IslandArray((Pointer)this).offsetAddress(i);
    }

	public native @ByRef @Name("operator =") IslandArray put(@Const @ByRef IslandArray other);
	public IslandArray() { super((Pointer)null); allocate(); }
	private native void allocate();

	/**Generally it is best to avoid using the copy constructor of an btAlignedObjectArray, and use a (const) reference to the array instead. */
	public IslandArray(@Const @ByRef IslandArray otherArray) { super((Pointer)null); allocate(otherArray); }
	private native void allocate(@Const @ByRef IslandArray otherArray);

	/** return the number of elements in the array */
	public native int size();

	public native @ByPtrRef btSimulationIslandManagerMt.Island at(int n);

	public native @ByPtrRef @Name("operator []") btSimulationIslandManagerMt.Island get(int n);

	/**clear the array, deallocated memory. Generally it is better to use array.resize(0), to reduce performance overhead of run-time memory (de)allocations. */
	public native void clear();

	public native void pop_back();

	/**resize changes the number of elements in the array. If the new size is larger, the new elements will be constructed using the optional second argument.
	 * when the new number of elements is smaller, the destructor will be called, but memory will not be freed, to reduce performance overhead of run-time memory (de)allocations. */
	public native void resizeNoInitialize(int newsize);

	public native void resize(int newsize, @ByPtrRef btSimulationIslandManagerMt.Island fillData/*=btSimulationIslandManagerMt::Island*()*/);
	public native void resize(int newsize);
	public native @ByPtrRef btSimulationIslandManagerMt.Island expandNonInitializing();

	public native @ByPtrRef btSimulationIslandManagerMt.Island expand(@ByPtrRef btSimulationIslandManagerMt.Island fillValue/*=btSimulationIslandManagerMt::Island*()*/);
	public native @ByPtrRef btSimulationIslandManagerMt.Island expand();

	public native void push_back(@ByPtrRef btSimulationIslandManagerMt.Island _Val);

	/** return the pre-allocated (reserved) elements, this is at least as large as the total number of elements,see size() and reserve() */
	public native @Name("capacity") int _capacity();

	public native void reserve(int _Count);

	/**heap sort from http://www.csse.monash.edu.au/~lloyd/tildeAlgDS/Sort/Heap/ */ /*downHeap*/

	public native void swap(int index0, int index1);

	/**non-recursive binary search, assumes sorted array */
	public native int findBinarySearch(@ByPtrRef btSimulationIslandManagerMt.Island key);

	public native int findLinearSearch(@ByPtrRef btSimulationIslandManagerMt.Island key);

	// If the key is not in the array, return -1 instead of 0,
	// since 0 also means the first element in the array.
	public native int findLinearSearch2(@ByPtrRef btSimulationIslandManagerMt.Island key);

	public native void removeAtIndex(int index);
	public native void remove(@ByPtrRef btSimulationIslandManagerMt.Island key);

	//PCK: whole function
	public native void initializeFromBuffer(Pointer buffer, int size, int _capacity);

	public native void copyFromArray(@Const @ByRef IslandArray otherArray);
}
